import Container from "../../../components/Container"
import Header from "../../../components/Header"
import Subheader from "../../../components/Subheader"
import Text from "../../../components/Text"
import ExternalText from "../../../components/ExternalText"
import Quotes from "../../../components/Quotes"
import Images from "../../../components/Images"
import CodeSnippets from "../../../components/CodeSnippets"
import Iframes from "../../../components/Iframes"
import AudioPlayer from "../../../components/AudioPlayer"
import VideoPlayer from "../../../components/VideoPlayer"
import Links from "../../../components/Links"
import P5Sketch from "../../../components/P5Sketch"
import ImagesCarousel from "../../../components/ImagesCarousel"

const BlogPost = () => {
  return (
    <Container>
      <div>
        <Header>Week 11</Header>
        <Subheader>
          Further Further Thoughts (More like no Thoughts) on the Cornell Box.
        </Subheader>
        <Text>
          We've just been having lots of thoughts around the idea of trapping
          sound but nothing seems to have stuck so far. We keep having second
          thoughts about the ideas due to the complexity of some of them and
          conceptually we weren't satisfied, the ideas so far have felt too
          straightforward and literal and we are more interest in pushing the
          concept further rather than just make something pretty and simple.
          <br></br>
          <br></br>
          Taking a break from thinking about this project over thanksgiving, we
          have some ideas but we're gonna let them stew for a bit and see if we
          like it when we come back to it.
          <br></br>
          <br></br>
          This week's in-class tutorials were great to learn more about the
          visuals in unity, creating the material textures and triggering
          different animations/playback. This is where I can see things getting
          complicated but also very interesting. Especially being able to play
          with different textures and linking different component triggers. Also
          I don't usually think in 3D, but starting to "world build" in 3D and
          just to navigate through 3D needs some getting used to but should be
          something manageable.
        </Text>
        ¸
      </div>
    </Container>
  )
}

export default BlogPost
